<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.ale.proyectofinal.confirmacion">

    <Button
        android:id="@+id/confirmar_cand"
        android:layout_width="match_parent"
        android:layout_height="140sp"
        android:background="@drawable/button" />

    <TextView
        android:id="@+id/confirmar_nombreCand"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textSize="18sp"
        android:layout_alignTop="@id/confirmar_cand"
        android:layout_alignParentRight="true"
        android:paddingRight="1dp"/>

    <TextView
        android:id="@+id/confirmar_partidoCand"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textSize="18sp"
        android:layout_below="@id/confirmar_nombreCand"
        android:layout_alignParentRight="true"
        android:paddingRight="1dp"/>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/contact"
        android:layout_alignTop="@id/confirmar_cand"
        android:layout_alignBottom="@id/confirmar_cand"
        android:layout_alignLeft="@id/confirmar_cand"
        android:background="@color/material_blue_grey_950"/>
    <Button
        android:id="@+id/btn_confirmar"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Confirmar"
        android:layout_below="@id/confirmar_cand"
        android:layout_alignParentLeft="true"
        android:onClick="attemptConfirm" />

    <Button
        android:id="@+id/btn_regresar"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Regresar"
        android:layout_below="@id/confirmar_cand"
        android:layout_alignParentRight="true"
        android:onClick="goBack"/>

    <TextView
        android:id="@+id/confirmarTest"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:layout_below="@id/btn_regresar" />

</RelativeLayout>